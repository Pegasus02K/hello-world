#include <stdio.h>
#include <stdlib.h>

int main(void)
{
	char *str2;
	str2 = malloc(100*sizeof(char));

	printf("The second C file for hello-world gitHb. changed by branch\nshow your word: ");
	scanf("%s",str);
	printf("I accepted your words: %s\n",str);
	// change done
	return;
}
